def test_successful_update_self_pet_info(self, name='Мурзик',
                                         animal_type='Котэ', age=5):
   _, auth_key = self.pf.get_api_key(valid_email, valid_password)
   _, my_pets = self.pf.get_list_of_pets(auth_key, "my_pets")

   if len(my_pets['pets']) > 0:
       status, result = self.pf.update_pet_info(auth_key, my_pets['pets'][0]['id'],
                                                name, animal_type, age)
       assert status == 200
       assert result['name'] == name
   else:
       raise Exception("There is no my pets")
'''Вначале объявляем тест, в названии которого указываем ожидаемый результат и метод, 
который тестируем. В тесте запрашиваем ключ api key, сохраняем в переменную auth_key 
и используем его в последующих вызовах. На этом этапе, если мы успешно получили ключ 
api key, то статус этого запроса нам не важен, и поэтому мы ставим прочерк (нижнее 
подчёркивание). Запрашиваем список собственных питомцев и проверяем, что количество 
питомцев больше нуля. Если их больше нуля, то берём первого по списку (как вы помните, 
индексы начинаются с 0), передаём вторым параметром id этого питомца в метод 
update_pet_info. Но если питомцев нет (то есть их количество равно нулю), то выбрасываем 
исключение с поясняющим текстом. Тест завершается со статусом Failed.'''
